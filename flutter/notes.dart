//* select text field content when tapping
/*
_focusNode.addListener(() {
      if (_focusNode.hasFocus) {
        _controller.selection = TextSelection(
          baseOffset: 0,
          extentOffset: _controller.text.length,
        );
      }
    });
*/
//* regExp to detect number of decimal
/*
RegExp(r'^\d*\.?\d{0,' + numberOfDecimal.toString() + r'}')
*/